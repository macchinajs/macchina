import C from "./../../shared/constants.js"
import validate from 'mongoose-validator'
import mongoose from 'mongoose'
{{#imports}}
{{buildSchemas_parseImports this}}
{{/imports}}

{{#each schemaEntries}}
{{#if (ne @key 'imports')}}
// {{@key}} schema
///////////////////////////////////////////////////////////////////////////////
const {{@key}}Obj = {
  {{buildSchemas_parseEntry this}}
}
const {{@key}} = new mongoose.Schema({{@key}}Obj)

export const {{@key}}Schema = new mongoose.Schema({{@key}})
{{/if}}
{{/each}}
