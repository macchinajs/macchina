import C from "./../../shared/constants.js"
import validate from 'mongoose-validator'
import mongoose from 'mongoose'
{{#imports}}
{{buildSchemas_parseImports this}}
{{/imports}}

{{#each schemaEntries}}
{{#if (ne @key 'imports')}}
// {{@key}} schema
///////////////////////////////////////////////////////////////////////////////
const {{lowercase @key}}SchemaBase = {
  {{buildSchemas_parseEntry this}}
}

export const {{lowercase @key}}Schema = new mongoose.Schema({{lowercase @key}}SchemaBase)
{{/if}}
{{/each}}
