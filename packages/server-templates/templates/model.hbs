import mongoose from 'mongoose'
import { {{lowercase name}}Schema } from './schema.js'
{{#if hasHooks}}
import hooks from './schemaHooks.js'
{{/if}}

{{#if hasHooks}}
// hooks
///////////////////////////////////////////////////////////////////////////////
for (let hook in hooks) {
  for (let hookmethod in hooks[hook]) {
    {{lowercase name}}Schema[hook](hookmethod, hooks[hook][hookmethod])
  }
}
{{/if}}

const {{name}} = mongoose.model('{{name}}', {{lowercase name}}Schema)
export default {{name}}



